mutation LinkAppToEnvironment($input: LinkAppToEnvironmentInput!) {
  linkAppToEnvironment(input: $input) {
    appId
    orgId
    slug
    name
    splitMode
    deploymentTarget
    gameApiUrl
    status
  }
}

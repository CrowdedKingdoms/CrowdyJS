mutation DestroyEnvironment($input: DestroyEnvironmentInput!) {
  destroyEnvironment(input: $input) {
    id
    environmentId
    kind
    status
    requestedBy
    error
    createdAt
    updatedAt
    finishedAt
  }
}

query UsersByGamertag($gamertag: String!) {
  usersByGamertag(gamertag: $gamertag) {
    userId
    email
    gamertag
    disambiguation
    isConfirmed
    createdAt
    grantEarlyAccess
    grantEarlyAccessOverride
    orgId
    externalId
    userType
  }
}

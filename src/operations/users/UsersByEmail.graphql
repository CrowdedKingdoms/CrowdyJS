query UsersByEmail($email: String!) {
  usersByEmail(email: $email) {
    userId
    email
    gamertag
    disambiguation
    isConfirmed
    createdAt
    grantEarlyAccess
    grantEarlyAccessOverride
    orgId
    externalId
    userType
  }
}
